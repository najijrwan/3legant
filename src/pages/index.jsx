import { Layout } from '@layout';
import { HeroSlider } from '@home';

const HomePage = () => {
    return (
        <Layout>
            <HeroSlider />
        </Layout>
    );
};

export default HomePage;