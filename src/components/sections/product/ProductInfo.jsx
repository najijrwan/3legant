export const ProductInfo = ({rating, reviews, label, details, discountPrice, price}) => {
    return (
        <div className="pb-6 flex flex-col gap-4">
            <div className="flex gap-2.5">
                {Array.map}
            </div>
        </div>
    )
}